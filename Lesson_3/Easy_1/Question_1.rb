# Question 1

# What would you expect the following code to output?

numbers = [1, 2, 2, 3]
numbers.uniq
puts numbers

# each element of the array will be printed to the screen on a newline. Array#uniq does not
# mutate the object.